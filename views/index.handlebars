<h1>Eat Da Burger</h1>

<h2>Burgers on the menu today:</h2>

<ul>
    {{#each burgers}}
    {{#unless devoured}}

    <li>
        <img src="/assets/img/burger.png" alt="burger" class="burger-img" width="80" height="80">

        {{burger_name}}

        <button class="devour" data-id="{{id}}" data-devoured="{{devoured}}">DEVOUR</button>
    </li>
    {{/unless}}
    {{/each}}

</ul>

<h3>Add a burger to the menu!</h3>
<form id="create-burger">
    <div class="form-group">
        <label for="bur">Burger Name:</label>
        <input type="text" id="bur" name="name">
    </div>
    <button type="submit">Add burger</button>
</form>